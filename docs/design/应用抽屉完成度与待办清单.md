# 应用抽屉完成度与待办清单

## 当前已完成（第一轮骨架）
- 搜索框 UI 与焦点切换（含键盘调起/收起）。
- 非搜索态居中列表（选中项居中放大）与分页基础能力。
- 右侧导轨基础交互（逐项切换）。
- 抽屉与 HOME/SETTINGS 的页面切换基础行为。

## 当前未完成（需进入后续迭代）
- 正式像素输入层（当前仍使用系统输入法代理输入）。

## 必修问题（本轮已修）
- 搜索有结果时，点击结果项不应先退出搜索；应直接选中或启动。
- 中文检索：拼音全拼匹配。
- 中文检索：拼音首字母匹配。
- 排序轻修正：频率/最近使用对默认序与搜索结果的轻度影响。
- 非拉丁索引策略：避免简单归并到 `Z` 导致可达性差。

## 测试现状
- 已补：搜索有结果时点击非选中项直接选中。
- 已补：搜索有结果时点击当前选中项直接启动。
- 已补：拼音全拼命中。
- 已补：拼音首字母命中。
- 已补：非拉丁索引行为。
- 已补：频率轻修正排序效果。
