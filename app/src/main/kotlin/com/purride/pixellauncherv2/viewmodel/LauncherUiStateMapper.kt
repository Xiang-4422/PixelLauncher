package com.purride.pixellauncherv2.viewmodel

import com.purride.pixellauncherv2.launcher.LauncherState

fun LauncherState.toLauncherUiState(): LauncherUiState = LauncherUiState(
    apps = apps,
    drawerVisibleApps = drawerVisibleApps,
    drawerQuery = drawerQuery,
    isDrawerSearchFocused = isDrawerSearchFocused,
    isDrawerRailSliding = isDrawerRailSliding,
    isAppActionMenuVisible = isAppActionMenuVisible,
    selectedIndex = selectedIndex,
    listStartIndex = listStartIndex,
    drawerPageIndex = drawerPageIndex,
    drawerFocus = drawerFocus,
    isLoading = isLoading,
    currentTimeText = currentTimeText,
    currentDateText = currentDateText,
    currentWeekdayText = currentWeekdayText,
    mode = mode,
    returnMode = returnMode,
    settingsSelectedIndex = settingsSelectedIndex,
    settingsListStartIndex = settingsListStartIndex,
    appEditorSelectedIndex = appEditorSelectedIndex,
    appEditorNameDraft = appEditorNameDraft,
    appEditorAliasDraft = appEditorAliasDraft,
    unreadSmsEntries = unreadSmsEntries,
    smsSelectedIndex = smsSelectedIndex,
    smsListStartIndex = smsListStartIndex,
    smsThreads = smsThreads,
    isSmsThreadsLoading = isSmsThreadsLoading,
    smsThreadSelectedIndex = smsThreadSelectedIndex,
    smsThreadListStartIndex = smsThreadListStartIndex,
    smsCurrentThreadId = smsCurrentThreadId,
    smsCurrentAddress = smsCurrentAddress,
    smsMessages = smsMessages,
    smsThreadSearchQuery = smsThreadSearchQuery,
    smsDraftText = smsDraftText,
    smsSendStatusText = smsSendStatusText,
    isDefaultSmsApp = isDefaultSmsApp,
    smsPermissionState = smsPermissionState,
    selectedPixelShape = selectedPixelShape,
    selectedDotSizePx = selectedDotSizePx,
    selectedPixelSizePresetIndex = selectedPixelSizePresetIndex,
    isPixelGapEnabled = isPixelGapEnabled,
    selectedTheme = selectedTheme,
    drawerListAlignment = drawerListAlignment,
    isIdlePageEnabled = isIdlePageEnabled,
    chargeAutoIdleEnabled = chargeAutoIdleEnabled,
    inactivityAutoIdleEnabled = inactivityAutoIdleEnabled,
    idleTimeoutSeconds = idleTimeoutSeconds,
    openDrawerInSearchMode = openDrawerInSearchMode,
    chargeIdleEffect = chargeIdleEffect,
    batteryLevel = batteryLevel,
    isCharging = isCharging,
    recentApps = recentApps,
    lastInteractionUptimeMs = lastInteractionUptimeMs,
    launchCount = launchCount,
    lastLaunchPackageName = lastLaunchPackageName,
    nextAlarmText = nextAlarmText,
    missedCallCount = missedCallCount,
    unreadSmsCount = unreadSmsCount,
    notificationSummaryText = notificationSummaryText,
    notificationCount = notificationCount,
    notificationSources = notificationSources,
    mutedNotificationSourceIds = mutedNotificationSourceIds,
    priorityNotificationSourceIds = priorityNotificationSourceIds,
    rainHintText = rainHintText,
    rainUpdatedTimeText = rainUpdatedTimeText,
    screenUsageTimeText = screenUsageTimeText,
    screenOpenCountText = screenOpenCountText,
    statusBarMessageText = statusBarMessageText,
    statusBarActionLeadingText = statusBarActionLeadingText,
    statusBarActionLabel = statusBarActionLabel,
    isStatusBarActionDanger = isStatusBarActionDanger,
    hasUsageAccess = hasUsageAccess,
    hasLocationPermission = hasLocationPermission,
    hasCallLogPermission = hasCallLogPermission,
    hasSmsReadPermission = hasSmsReadPermission,
    hasPostNotificationPermission = hasPostNotificationPermission,
    hasNotificationListenerAccess = hasNotificationListenerAccess,
    dataHealthUpdatedTimeText = dataHealthUpdatedTimeText,
)
