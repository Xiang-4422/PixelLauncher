package com.purride.pixellauncherv2.render

/**
 * 兼容层别名。
 *
 * `IdleMaskFrame` 的真实定义已经迁到 `:pixel-core`，
 * 当前先保留旧包名入口，降低第一轮模块拆分的改动面。
 */
typealias IdleMaskFrame = com.purride.pixelcore.IdleMaskFrame
