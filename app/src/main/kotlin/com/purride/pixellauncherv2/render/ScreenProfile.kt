package com.purride.pixellauncherv2.render

/**
 * 兼容层别名。
 *
 * `ScreenProfile` 的真实定义已经迁到 `:pixel-core`，
 * 当前先保留旧包名，避免第一轮模块拆分就触发大面积 import 改动。
 */
typealias ScreenProfile = com.purride.pixelcore.ScreenProfile

/**
 * 兼容层别名。
 */
typealias PixelShape = com.purride.pixelcore.PixelShape

/**
 * 兼容层别名。
 */
typealias ScaleMode = com.purride.pixelcore.ScaleMode
