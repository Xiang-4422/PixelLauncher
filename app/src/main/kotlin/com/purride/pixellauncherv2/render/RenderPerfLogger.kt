package com.purride.pixellauncherv2.render

/**
 * 兼容层别名。
 *
 * `RenderPerfLogger` 的真实实现已经迁到 `:pixel-core`，
 * 当前先保留旧包名入口，避免第一轮拆分改动过大。
 */
typealias RenderPerfLogger = com.purride.pixelcore.RenderPerfLogger
