# pixel-engine 演进路线

> SDK 的演进方向、当前迭代任务、优先级、历史决策。给接手维护 `pixel-engine` 的下一位开发者，也给规划下一阶段时的自己。
>
> 本文档是当前事实版路线图，不是历史计划的逐字继承。任何任务都需要先按代码事实复核；只有仍然合理、仍然能提升 SDK 可信度或能力的任务才执行。

最后更新：2026-05-25

---

## 1. 当前结论

`pixel-engine` 已经过了“能不能跑”的阶段，当前重点是产品化：API 可信、文档准确、性能有门禁、demo 能当 release gate。

当前已具备：

- 单模块 `:pixel-engine`，内部保留 `pixelcore` / `pixelui` / `pixelengine` 包边界。
- Flutter 风格 `Widget -> Element -> RenderObject` 三层模型。
- `PixelHostView` 继承 `android.view.View`，不是 `SurfaceView`。
- `explicitApi = Strict` 已启用。
- `pixel-demo` 已覆盖大量公开 widget 和高级扩展场景。
- `ListViewBuilder(itemExtent != null)` 已有固定高度 lazy 路径；`ListViewBuilder(estimatedItemExtent != null)` 已有显式 opt-in 的变高 lazy v1；`itemExtent == null && estimatedItemExtent == null` 仍是 eager 兼容路径。
- `ListViewSeparatedBuilder` 已支持固定 item/separator 高度的 lazy 分隔列表。
- 文本渲染已引入 internal paragraph layout，`Text` / `RichText` 共用测量、断行和 ellipsis 语义；`PixelTextStyle` 已支持 `letterSpacing`、`lineHeight`、整数 `fontScale`。
- `NestedScrollSession` 已作为 internal 手势 session 状态容器接入，`PixelHostView` 仍保留主要触摸仲裁逻辑。
- 主题状态矩阵已有基础单测，`THEME_STATES` demo 已作为状态样式 gate。
- `PerfBaselineTest` 默认只打印样本，不写 tracked baseline；只有 `REGEN_PERF_BASELINE=1` 时写盘；`PerfRegressionTest` 已提供只读阈值门禁。
- `PublicApiCoverageTest` 已检查公开 widget API 至少出现在 docs / demo / tests 中。
- 动画系统 Layer 1（核心）+ Layer 2（隐式 widget）已落地（任务 #15 ✅）。
- 彩色模式与 Mono 模式双路径并存已落地（任务 #16 ✅）。
- `pixel-demo` 已重构为单 `DemoActivity` + `DemoCatalog` + 6 组场景，旧 `DemoScenes.kt` 已删除（任务 #10 ✅）。

当前固定验收命令：

```bash
./gradlew :pixel-engine:testDebugUnitTest :pixel-engine:assembleDebug :pixel-demo:assembleDebug --no-daemon
```

如果改到 lint / public API，再加：

```bash
./gradlew :pixel-engine:lintDebug --no-daemon
```

---

## 2. 阅读路径

接手前按顺序读：

1. [`pixel-engine/README.md`](../../README.md)
2. [快速入门](../使用说明/快速入门.md)
3. [架构说明](../实现说明/架构说明.md)
4. [组件目录](../使用说明/组件目录.md)
5. [主题系统](../使用说明/主题系统.md)
6. [状态管理](../使用说明/状态管理.md)
7. [扩展开发](../使用说明/扩展开发.md)

重点源码入口：

- `pixel-engine/src/main/kotlin/com/purride/pixelui/widgets/PixelWidgets.kt`
- `pixel-engine/src/main/kotlin/com/purride/pixelui/internal/widgets/scroll/ListWidgets.kt`
- `pixel-engine/src/main/kotlin/com/purride/pixelui/internal/render/pipeline/core/RenderObjectWidget.kt`
- `pixel-engine/src/main/kotlin/com/purride/pixelui/advanced/AdvancedWidgetAliases.kt`
- `pixel-engine/src/test/kotlin/com/purride/pixelui/perf/PerfBaselineTest.kt`
- `pixel-engine/src/test/kotlin/com/purride/pixelui/perf/PerfRegressionTest.kt`
- `pixel-engine/src/test/kotlin/com/purride/pixelui/regression/PublicApiCoverageTest.kt`
- `pixel-demo/src/main/kotlin/com/purride/pixeldemo/catalog/DemoCatalog.kt`（scene 目录）

---

## 3. P0：已完成

P0 三项已按独立提交完成，保留在这里作为历史验收记录。

### 任务 #1：`advanced` 包稳定性声明

#### 为什么先做

`com.purride.pixelui.advanced` 已把 RenderObject 扩展点通过 typealias 暴露给 SDK 使用者，但目前文档没有明确哪些类型稳定、哪些还在实验期。这个任务不改行为，风险最低，却能提高 SDK 可信度。

#### 已完成

1. `AdvancedWidgetAliases.kt` 顶部 KDoc 已说明 advanced API 的稳定性分级。
2. 每个 typealias 已标注稳定性说明。
3. [扩展开发](../使用说明/扩展开发.md) 已增加“API 稳定性”章节。

#### 建议分级

| Typealias | 建议等级 | 说明 |
|---|---|---|
| `PixelRenderObjectWidget` | Stable | RenderObject widget 基类 |
| `PixelLeafRenderObjectWidget` | Stable | 已是独立 leaf 子类 |
| `PixelSingleChildRenderObjectWidget` | Stable | 单 child widget 基类 |
| `PixelMultiChildRenderObjectWidget` | Stable | 多 child widget 基类 |
| `PixelRenderObject` | Stable | render object 根基类 |
| `PixelRenderBox` | Stable | 盒模型基类 |
| `PixelRenderConstraints` | Stable | 布局约束值对象 |
| `PixelRenderSize` | Stable | 布局尺寸值对象 |
| `PixelSingleChildRenderObject` | Experimental | 子节点协议后续可能收敛 |
| `PixelMultiChildRenderObject` | Experimental | 子节点协议后续可能收敛 |
| `PixelPaintContext` | Experimental | buffer pool / paint context 可能继续扩展 |
| `PixelHitTestResult` | Experimental | hitTest API 仍可能演进 |
| `PixelRenderObjectWithChild` | Experimental | 协议类扩展点 |
| `PixelRenderObjectWithChildren` | Experimental | 协议类扩展点 |

#### 验收

```bash
./gradlew :pixel-engine:testDebugUnitTest :pixel-engine:lintDebug --no-daemon
```

---

### 任务 #2：`ListViewSeparatedBuilder` 固定高度 lazy 版本

#### 状态

已新增 `ListViewSeparatedBuilder` 固定高度 lazy API，保留 `ListViewSeparated` eager API。

#### 动机

`ListViewSeparated` 当前是 eager：会一次性构造全部 item 和 separator。长列表使用它会有首屏构建成本风险。

`ListViewBuilder` 已有固定高度 lazy 路径，但只支持同一高度的 item。`ListViewSeparatedBuilder` 应该补齐“item 固定高度 + separator 固定高度”的 lazy 场景。

#### API 草案

```kotlin
public fun ListViewSeparatedBuilder(
    itemCount: Int,
    itemBuilder: (Int) -> Widget,
    separatorBuilder: (Int) -> Widget,
    state: PixelListState,
    controller: PixelListController,
    itemExtent: Int,
    separatorExtent: Int,
    cacheExtent: Int = 1,
    key: Any? = null,
): Widget
```

#### 设计要求

- 保留现有 `ListViewSeparated` eager API，不做破坏性修改。
- `itemExtent` 和 `separatorExtent` 都是必填，避免把变高虚拟化伪装成已完成。
- 内部需要正确计算交错虚拟行：
  - virtual index 偶数：item，真实 index = virtual / 2，高度 = `itemExtent`
  - virtual index 奇数：separator，真实 index = virtual / 2，高度 = `separatorExtent`
- 不能直接复用现有 `RenderLazyListViewport` 的 `itemExtent + spacing` 模型，除非先把它抽象成“按 virtual index 取 extent”的通用模型；否则 item/separator 不同高度会算错 offset。
- `state.itemTopOffsetsPx` 和 `state.itemHeightsPx` 仍应按真实 item 维度同步，保证 `scrollItemIntoView(state, itemIndex)` 定位真实 item，而不是 separator。

#### 文件

- `pixel-engine/src/main/kotlin/com/purride/pixelui/widgets/PixelWidgets.kt`
- `pixel-engine/src/main/kotlin/com/purride/pixelui/internal/widgets/scroll/ListWidgets.kt`
- `pixel-engine/src/main/kotlin/com/purride/pixelui/internal/render/pipeline/renderobjects/viewport/RenderScrollViewport.kt`
- `pixel-engine/src/test/kotlin/com/purride/pixelui/internal/render/pipeline/PipelineElementTreeRendererTest.kt`
- `pixel-engine/docs/组件目录.md`
- `pixel-demo/src/main/kotlin/com/purride/pixeldemo/catalog/DemoCatalog.kt`（scene 目录） 的 `PALETTE_GRID` 场景

#### 验收

- 1000 item 初始 render 只调用可见窗口 + cache 范围内的 `itemBuilder` / `separatorBuilder`。
- 点击 target 和 list target 仍被 viewport 裁剪。
- `scrollItemIntoView` 对真实 item index 生效。
- `ListViewSeparated` eager API 行为不变。
- demo `PALETTE_GRID` 改用新 builder。

验证：

```bash
./gradlew :pixel-engine:testDebugUnitTest :pixel-demo:assembleDebug --no-daemon
```

---

### 任务 #3：性能回归阈值门禁

#### 当前事实

`PerfBaselineTest` 现在只负责采样和打印。默认不写盘，`REGEN_PERF_BASELINE=1` 才覆盖 baseline。这部分已经修正，不要再按旧文档恢复默认写盘。

#### 状态

已新增 `PerfRegressionTest`，并将测量逻辑抽到共享 helper；默认仍不写 baseline。

#### 设计要求

- 与 `PerfBaselineTest` 共享测量 helper，避免两套测量逻辑漂移。
- 默认永不写盘。
- baseline 文件不存在时 skip，并打印如何生成 baseline。
- JVM 不支持线程分配统计时 skip allocation 相关断言，不要 fail。
- 每项指标建议多轮采样取中位数，降低本机波动。
- 阈值先保守，避免 CI 噪声：
  - allocation：baseline * 1.5
  - render time：baseline * 2.0
  - blit：baseline * 2.0
  - rich text：baseline * 2.0

#### 验收

- 当前 baseline 下通过。
- 故意制造明显回归时失败信息要显示 baseline/current/threshold。
- 不修改 `perf-baseline.json`。

验证：

```bash
./gradlew :pixel-engine:testDebugUnitTest --no-daemon
```

---

## 4. P1：已完成首轮

P1 的首轮能力实现已完成，保留本节作为当前状态和后续增量边界。后续继续迭代时，仍应先按设计文档复核目标、非目标、API、数据结构、兼容策略和测试计划。

### 任务 #4：变高 item 列表虚拟化

当前 `ListViewBuilder(itemExtent != null)` 是固定高度 lazy；`ListViewBuilder(estimatedItemExtent != null)` 是变高 lazy v1；`itemExtent == null && estimatedItemExtent == null` 仍是 eager。

设计文档：[变高列表虚拟化](../实现说明/变高列表虚拟化.md)。

已完成：

- 新增 `estimatedItemExtent` 作为显式 opt-in，不改变 eager 兼容行为。
- 增加 item 高度测量缓存。
- 对未测 item 使用估算高度。
- `scrollItemIntoView` 支持已测和未测 item 定位。
- demo 已增加 `VARIABLE_HEIGHT_LIST`。

后续增量：

- 补 5000 item perf 场景和阈值。
- 补 item 高度动态变化后的二次稳定测试。
- 优化远端未测 item 定位后的下一帧微调策略。
- 不要把变高 `ListViewSeparatedBuilder` 混进本能力；需要时另写设计。

不要把该任务和 `ListViewSeparatedBuilder` 混在一个提交里。

---

### 任务 #5：文本系统 v2

当前文本系统已引入 internal paragraph model，`Text` / `RichText` 共用测量、断行、ellipsis 和 line metrics；`TextField` placeholder 也复用统一文本渲染路径。

设计文档：[文本系统 v2](../实现说明/文本系统-v2.md)。

已完成：

1. internal paragraph model：统一 `RenderText` / `RenderRichText` 测量、断行、ellipsis。
2. `PixelTextStyle` 扩展：`letterSpacing`、`lineHeight`、受限的整数倍 `fontScale`。
3. 富文本 demo 已扩展样式展示。

后续增量：

- TextField selection 可视化与光标节拍。
- IME composition 显示与提交语义。
- 复杂字体 fallback 与 glyph fallback 策略。
- 为新增文本样式补更多 golden / snapshot 场景。

每阶段都需要 golden 或 snapshot 回归。

---

### 任务 #6：滚动 / 手势 v2

当前已有基本 fling、bounce 参数、Pager/List 嵌套仲裁；`NestedScrollSession` 已作为 internal 状态容器接入，但 `PixelHostView` 中仍保留主要 move/up/cancel 仲裁逻辑。

设计文档：[滚动手势 v2](../实现说明/滚动手势-v2.md)。

已完成：

1. `PixelScrollPhysics` 明确 snap / overscroll / edge handoff 语义。
2. 新增 `NestedScrollSession`，把一次手势的归属状态显式建模。
3. demo 已增加 gesture stress 场景：`PageView + ListView + TextField`。

后续增量：

- 把 `PixelHostView.onTouchEvent` 中的 move/up/cancel 仲裁迁到 `PixelHostGestureRouter`。
- 让 `NestedScrollSession` 记录 consumed delta / remaining delta / edge handoff 状态。
- 补 emulator 或真机手测记录，覆盖 pager/list/text input 同屏拖动、fling、cancel。

该任务会触碰 `PixelHostView` 手势分发，必须配真机手测。

---

## 5. P2：已完成基础门禁，继续产品化

### 任务 #7：主题系统产品化

当前 theme token 已能覆盖主要状态，按钮、容器、输入框的状态矩阵已有基础单测；`THEME_STATES` demo 已作为可视 gate。

已完成：

- 文档固定优先级：显式 style > theme component style > tokens > hardcoded default。
- 基础单测覆盖 disabled / pressed / selected / focused / readOnly。
- demo `THEME_STATES` 展示状态矩阵。

后续增量：

- 把状态矩阵扩展到更多组件组合和局部 `Theme` 覆盖场景。
- 补 element snapshot 或 golden，避免状态样式只停留在 resolver 单测。
- 明确 selected / pressed 同时存在时的优先级并补测试。

---

### 任务 #8：demo 作为 release gate

目标：新增 public widget 或公开 API 时，必须同时有：

- 至少一个 demo scene 或现有 scene 扩展。
- 至少一个单测。
- 视觉相关能力有 golden 或 element snapshot。

已完成：

- `PublicApiCoverageTest`：扫描 widgets / advanced / controllers / foundation / state / gesture / host 七个包，检查 class / object / typealias / 大写 fun 是否至少在 docs / tests / demo 中出现。demo 端使用严格调用规则（`Name(`），仅 import 不计为覆盖。
- `DemoCatalogCoverageTest`：验证 6 组 31 个 scene 全部注册、id 唯一、title / description 非空、`findById` 可定位所有 scene。
- `pixel-demo` 已重构为单 `DemoActivity` + 6 组 31 个 showcase scene，菜单本身也用 pixel-engine 渲染。

固定 release gate：

```bash
./gradlew :pixel-engine:testDebugUnitTest :pixel-engine:lintDebug :pixel-engine:assembleDebug :pixel-demo:assembleDebug :pixel-demo:testDebugUnitTest --no-daemon
```

---

## 6. P3：维护性清理

### 任务 #9：拆分 `PixelHostView.kt`

`PixelHostView.kt` 仍偏大，混合 View 生命周期、手势、IME、runtime 管理。当前只拆出了 `PixelHostFrameLoop`，后续继续纯重构拆分：

- `PixelHostView.kt`：View 生命周期 + onDraw。
- `PixelHostGestureRouter.kt`：手势分发和 `NestedScrollSession` 消费。
- `PixelHostTextInputCoordinator.kt`：输入焦点和 IME 桥接。
- `PixelHostFrameLoop.kt`：frame delta（已拆出）；active pager/list step 后续可继续迁出。

要求不改公开 API，不改行为。

---

### 任务 #10：拆分 `DemoScenes.kt`（已完成）

`pixel-demo` 已重构为单 `DemoActivity` + `DemoCatalog` + 6 组 31 个独立 scene 文件，架构如下：

- `showcase/foundation/`（6 个场景）
- `showcase/interaction/`（5 个场景）
- `showcase/scroll/`（9 个场景）
- `showcase/theme/`（4 个场景）
- `showcase/extension/`（4 个场景）
- `showcase/integration/`（3 个场景）

旧 `DemoScenes.kt` / `DemoSceneKind.kt` / `DemoMenuActivity` / `DemoSceneActivity` 已全部删除。

---

### 任务 #11：包结构策略

当前是“扁平根包 + 少量子包”的混合结构，并且已在 [架构说明](../实现说明/架构说明.md) 说明。除非有强业务驱动，否则保持现状，不为美观做大规模 import 迁移。

---

## 7. 历史决策与注意事项

- 保留 `com.purride.pixelui` 扁平包作为主要用户入口。
- `DefaultTextRasterizer` 通过 inherited widget 注入，宿主切换字体只需 invalidate。
- `PixelListController` / `PixelPagerController` 已显式处理 NaN / Infinity 速度。
- `PerfBaselineTest` 默认不写盘，避免污染 tracked baseline。
- `ListViewBuilder` 固定高度 lazy 必须传 `itemExtent`；变高 lazy 必须传 `estimatedItemExtent`；两者都不传就是 eager。
- `PixelHostView` 是 Android `View`，不是 `SurfaceView`。
- 本文档（[演进路线](演进路线.md)）是维护参考，不是强制执行计划。执行前必须先按当前代码复核。

---

## 8. 下一轮优先任务

### 顺序决策：动画先于彩色

任务 #15（动画）与任务 #16（彩色）在 2026-05-20 评估后，**确认先做动画**：

- 风险面悬殊：动画是追加新组件，不动 `PixelBuffer` / blit / pool 等热路径；彩色必须动渲染层最底层存储格式。
- 痛点匹配：现有 demo / PixelLauncher 能感受到的体验缺口（按钮无按下反馈、主题切换瞬切、列表项瞬现、TextField 光标不闪）全部是动画问题，不是彩色问题。
- 依赖方向单向：动画在单色模式下落地简单；彩色后做时只需类比加 `PixelColorTween`，不破坏已有动画 API。
- 决策回退成本：动画选错默认曲线下版本改即可；彩色的"Buffer 拆双类型 vs 统一接口"等架构决策难回退。

**唯一耦合点**：动画 Layer 1 的 `Tween<T>` 抽象基类不要硬编码"输出端是 `PixelTone`"，必须为未来 `PixelColorTween` 留扩展位。具体在 [动画实现计划](动画实现计划.md) §3 设计取舍中已显式约束。

动画的具体执行步骤见 [动画实现计划](动画实现计划.md)。

### 任务 #12：手势 v2 实质收敛

当前 `NestedScrollSession` 只是状态容器，真正的拖动启动、handoff、fling/cancel 仲裁仍主要在 `PixelHostView.onTouchEvent`。

建议下一步优先做：

- 新增 `PixelHostGestureRouter`，接收 MotionEvent 的逻辑坐标、候选 targets、策略对象和 velocity。
- 把 pager/list/text input owner 锁定、edge handoff、up/cancel 收尾从 `PixelHostView` 迁出。
- 保持 `PixelHostView` 只负责 Android View 生命周期、坐标映射、invalidate 和桥接调用。
- 补单测覆盖：未过 touch slop 不锁 owner；list 可滚时优先 list；list 到边界后 handoff pager；TextField tap 聚焦但不抢拖动；cancel 清理 active owner。
- demo 使用 `GESTURE_STRESS` 做手动验收。

### 任务 #13：变高 lazy list 压测与校正

当前变高 lazy list v1 已可用，但还需要压实远端定位和性能：

- `PerfRegressionTest` 增加 1000/5000 item 变高 lazy 列表指标。
- 补 `scrollItemIntoView` 到远端未测 item 后的下一帧校正测试。
- 补 item 高度变化后 content height / maxScrollOffset / target 裁剪的回归。
- 确认 `measuredItemHeightsPx` 缩容、扩容和 itemCount 变化时不会保留脏缓存。

### 任务 #14：文本输入体验补全

当前 paragraph model 已稳定文本测量和绘制，输入体验仍需继续补：

- TextField 光标节拍和 selection 可视化。
- IME composition 显示和提交语义。
- 多行输入中的 selection clamp、换行、submit action 行为回归。
- 新增 demo 场景或扩展 `TEXT_INPUT_MULTILINE`。

### 任务 #15：动画系统落地（✅ 已完成，2026-05-20）

**Layer 1**（动画核心）和 **Layer 2**（隐式动画 widget）均已落地：

- `PixelTicker` / `PixelTickerProvider` / `PixelAnimationController` / `PixelAnimationStatus`
- `Tween<T>`：`IntTween` / `EdgeInsetsTween` / `OffsetTween` / `PixelToneTween` / `IntOffset`
- `Curves`：`Linear` / `EaseIn` / `EaseOut` / `EaseInOut` / `Step(n)` / `Interval`
- `CurvedAnimation`
- `TweenAnimationBuilder` / `AnimatedContainer` / `AnimatedOpacity` / `AnimatedPadding` / `AnimatedAlign` / `AnimatedPositioned` / `AnimatedSwitcher`
- Demo：`AnimationCoreScene` + `ImplicitAnimationsScene`（注册于 DemoCatalog "Animation" section）
- 单测：30+ test method，全绿

开放问题已收敛，见 [动画方向.md §6](动画方向.md)。Layer 3（显式工具）和 Layer 4（列表/路由过渡）留待后续任务。

### 任务 #16：彩色模式落地 ✅ 已完成（2026-05-20）

pixel-engine 已完整支持彩色（Color）模式，与 Mono 模式双路径并存：

- 用户可以指定任意 ARGB `PixelColor`；`PixelColorMode.Color` 全局开关。
- `PixelHostSetupConfig(colorMode = PixelColorMode.Color)` 启用，构造后不可变。
- `MonoPixelBuffer`（ByteArray）/ `ColorPixelBuffer`（IntArray）双类型隔离，不破坏现有 Mono 路径。
- `PixelThemeTokens` 双字段（`xxxTone` + `xxxColor`），4 个预设已填默认彩色映射。
- 所有容器 / 文本 / 按钮 widget 增加 `fillColor` / `borderColor` / `color` 参数。
- `PixelColorTween` 支持颜色补间动画。

详见 [主题系统 → 彩色模式](../使用说明/主题系统.md#彩色模式)。

色彩演进文档定义：

- 两种模式的语义和切换约束
- 现有概念（`PixelTone` / `PixelPalette` / tokens / `PixelBuffer`）在两种模式下的行为
- Phase A-E 的落地分层（渲染层基础设施 → 主题重构 → 公开 widget API → Mode 切换 → 文档验收）
- 7 个待决开放问题（PixelColor/PixelTone 互转语义、tokens 统一 vs 并存、Mono 下传 color 的处理、Buffer 拆双类型 vs 统一接口、彩色模式 palette 兼容、动态切换 mode、彩色模式预设主题）

落地前必须：

- 把 7 个开放问题在 issue / 讨论里收敛——尤其 Q4（Buffer 拆 vs 统一）会直接决定渲染层架构。
- Phase A 是最重的一步（buffer 存储 + blit + pool），建议单独立项；不要和公开 API 改动混提交。
- 不要在彩色模式落地前给现有 Mono widget 加 color 参数——会污染兼容窗口。

### 任务 #18：能力扩展 backlog（B 组）

把 SDK 能服务的场景从"驱动一个 Launcher"扩展到通用像素 UI 工具。每项独立提交：

- **B1 Image / Sprite / SpriteSheet / AnimatedSprite**：补 `PixelImage(asset | bitmap, mode = nearest)` 和 `AnimatedSprite(frames, fps)`。Launcher app icon 显示的硬依赖。
- **B2 图形原语 widget**（Line / Circle / Polygon / Path）：把常用形状沉淀成公开 widget，避免每次下到 `PixelLeafRenderObjectWidget`。app 端 `BatteryDividerWidget` 是当前缺口的实证。
- **B3 FutureBuilder / StreamBuilder**：异步数据 widget，配合 Flow / suspend；app 列表、电量、SMS 均为异步源。
- **B5 CustomScrollView / Sliver 家族**：至少 `SliverList` + `SliverAppBar`（header 跟随滚动收起的标准范式）。
- **B4 Navigator 栈式导航 + B7 Animation Layer 4 过渡**：成对落地。`PixelNavigator` + `PixelRoute` 配合 `PageRouteBuilder` 的 slide / fade。Hero 已在 [动画方向.md §6] 明确不做。
- **B6 Form 表单框架**：`Form` / `FormField<T>` / `Validator` + `FocusScope`，覆盖 Launcher 设置 / SMS 表单场景。

### 任务 #19：使用工学 backlog（C 组）

提升自身开发速度和定位 bug 速度：

- **C1 运行时调试工具**：debug-only `PixelDebugOverlay`（FPS / paint time / allocation）；`PixelHostSetup.dumpElementTree()` 公开 API。
- **C2 WidgetTester DSL**：`PixelTester.pumpWidget(w) → tap(find.byText("X")) → pumpAndSettle()`，让应用层端到端测试不再写底层 element tree 断言。
- **C3 资源加载规范化**：TTF/BDF → 点阵 converter（编译期 task）；JSON palette schema；配 B1 的 bitmap decoder。
- **C4 反向沉淀中级 widget**：把 app 端 `BatteryDividerWidget` / `SettingsControls` 等通用组件回流 SDK（依赖 B2 完成）。
- **C5 键盘 / 手柄输入**：`FocusNode` / `FocusTraversalPolicy`；KeyEvent 路由（目前仅 IME 用）。pixel 风 + 手柄是 retro 游戏天然组合。

### 任务 #20：运行时正确性 backlog（A 组，延后）

当前 `app` MainActivity 为 portrait-only + singleTask，`pixel-demo` DemoActivity 用 `configChanges` 自处理，本组实际不在自用路径上。**仅在准备对外发布、或自用场景改变时启动。**

- **A1 Lifecycle + 配置变更**：`LifecycleObserver` 桥接；`PixelListController` / `PixelPagerController` 接入 `onSaveInstanceState`。
- **A2 WindowInsets / edge-to-edge**：`PixelHostView` 暴露 system insets，新增 `SafeArea` widget。
- **A3 暗色模式响应**：监听 `Configuration.uiMode`，通过 inherited widget 注入到树触发 `ThemeData` 重算。

---

## 9. 工作建议

1. 每个任务先确认当前代码事实，再改。
2. 每个任务独立提交，不把功能、demo、文档、纯重构混在一起。
3. P1/P2 已有首轮实现，继续迭代前仍要按代码事实复核设计文档。
4. 改公开 API 必须同步 `README` 和 `docs/` 中对应文档。
5. 新增 widget 或公开 API 必须补 demo 和测试。
6. 不要在 `paint()` / hot path 中新增对象分配。
7. 不要从业务 app 反向污染 `pixel-engine`，engine 不应知道 Launcher 业务语义。
