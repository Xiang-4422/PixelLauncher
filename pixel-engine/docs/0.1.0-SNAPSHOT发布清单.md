# pixel-engine 0.1.0-SNAPSHOT 发布清单

本文记录当前 `0.1.0-SNAPSHOT` 的内部 SDK 发布状态。长期版本、弃用和迁移规则见
`pixel-engine/docs/发布与兼容策略.md`。

## 1. 发布定位

- artifact：`com.purride:pixel-engine:0.1.0-SNAPSHOT`
- 当前阶段：内部 `0.x` Android-only 像素 UI SDK。
- 兼容承诺：source / binary compatibility best-effort。
- 不承诺：Material / Cupertino 组件库、Flutter 全 API、跨平台。

## 2. 当前 SDK 面

- Runtime：widget tree、retained state、layout、render、input、focus、semantics、navigation、animation。
- Android host：`PixelHostView`、`PixelHostSetup`、IME、clipboard、back、haptic、saved state、lifecycle、insets。
- Testing：SDK 可用 `PixelTester`、finder、gesture、text input、pixel buffer 断言。
- Diagnostics：`PixelDebugOverlay`、inspector、frame stats、错误边界。
- Components：Text、RichText、TextField、buttons、ListTile、selection、slider、tabs、overlay feedback、empty state、slidable、scaffold。
- Resource：bitmap、sprite sheet、glyph pack、resource manifest、resource cache。

## 3. 本快照已记录的破坏性变更

根目录 `CHANGELOG.md` 已记录：

- `PixelHostView.backgroundColor -> bezelColor`
- `PixelHostView.pixelGridColor -> offPixelColor`
- `PixelPagerController.startDrag` 增加 `viewportSizePx`

## 4. 不纳入本快照的组件

以下组件已经评审，但暂不进入 `0.1.0-SNAPSHOT` public API：

- `Menu`
- `Popover`
- `Tooltip`
- `ShortcutHint`

延后原因见 `pixel-engine/docs/长期规划.md`。它们需要真实锚点、焦点、键盘/手柄、碰撞避让和业务文案场景后再设计。

## 5. 必跑门禁

默认无设备发布门禁：

```bash
./tools/pixel-release-check.sh
```

该门禁覆盖：

- public API baseline
- binary API baseline
- KDoc coverage
- pixel-engine 单测、lint、assemble
- pixel-demo 单测、assemble
- Maven local dry-run
- mkdocs build

## 6. 补充检查

SDK consumer smoke：

```bash
./tools/pixel-sdk-consumer-smoke.sh
```

性能和长期运行检查：

```bash
./tools/pixel-perf-smoke.sh
./tools/pixel-soak-test.sh
```

真实设备检查依赖 ADB，不作为默认门禁：

```bash
./tools/pixel-device-smoke.sh
```

## 7. 发布前核对

- [x] `pixel-engine/build.gradle.kts` 版本为 `0.1.0-SNAPSHOT`。
- [x] `CHANGELOG.md` 有当前快照条目。
- [x] public / binary API baseline 已提交。
- [x] 使用说明、架构文档、长期规划和发布策略一致。
- [x] 0.x 兼容策略已写入文档。
- [x] 1.0+ SemVer / 弃用 / migration guide 规则已写入文档。
- [x] 默认 release gate 可运行。
- [x] 发布前最后一次运行 `pixel-sdk-consumer-smoke.sh`。
- [ ] 如要发给真实设备试用，运行 `pixel-device-smoke.sh`。
