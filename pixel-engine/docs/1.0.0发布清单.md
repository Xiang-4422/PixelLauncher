# Pixel Engine 1.0.0 发布清单

本文是 1.0 本地候选的唯一核对入口；历史 0.1 snapshot 清单只保留追溯用途。真实外部发布项已按
2026-07-16 用户决定移交未来独立发布 Goal。

## 代码与文档

- [x] 所有模块版本统一为 `1.0.0`，无 `SNAPSHOT` 泄漏到正式 POM/module metadata。
- [x] 九个 artifact 的职责、依赖 DAG、AAR/POM/sources/Javadoc/consumer rules 已验证。
- [x] public/Metalava/JVM ABI baseline 已审阅，stable/internal 边界无泄漏。
- [x] public/protected KDoc 门槛为 100%，严格 MkDocs 包含全部 SDK 文档。
- [x] Quickstart、Host、主题、路由、资源、SPI、测试、帧诊断和迁移指南已提供。
- [x] `CHANGELOG.md` 包含完整 `1.0.0 - Unreleased` 候选条目；最终发布日期留给未来发布 Goal。

## 行为与兼容

- [x] JVM、golden、property/fuzz、Lint、API 24/29/36 instrumentation 全部通过。
- [x] 最低/推荐工具链、聚合与九坐标隔离 Maven/R8 consumer 通过。
- [x] Demo 覆盖 Host、主题、组件、路由、输入、Accessibility、动画与诊断场景。

性能基准、趋势、trace 与 soak 已按 2026-07-16 用户决定移交后续独立 Goal，不属于当前发布清单。

## 发布与供应链

- [x] 固定许可证、NOTICE、源码归属与生成数据许可证清单。
- [x] 配置内存签名、远程 staging、下载后校验和不可变版本回滚步骤；真实 close/publish 留待最终授权。
- [x] CycloneDX SBOM、Release 依赖锁定、Gradle 校验元数据、OSV、secret/backup 扫描全部通过。
- [x] 准备稳定 required workflow、只读 GitHub 就绪检查器和 tag-bound Pages 部署工作流。
- [x] 未来发布预检会拒绝未绑定 required gate、未启用 Pages、缺失 tag/Release 和未清除 Support 状态。

以下真实发布事项不属于当前清单：管理员绑定 required gate、启用 Pages、正式 tag/Release notes、
文档站 URL、正式仓库、已验证 namespace、长期签名身份和真实 publish。

无凭据总门禁：

```bash
bash tools/pixel-release-check.sh
```

本清单完成只表示 `1.0.0` 本地候选通过，不表示已经对外发布。供应链实现和复核步骤见
[发布元数据与供应链](supply-chain.md)，支持工具链见[支持矩阵](support-matrix.md)。
