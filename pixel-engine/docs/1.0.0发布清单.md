# Pixel Engine 1.0.0 发布清单

真实外部发布已移交未来独立发布 Goal；本清单只定义本地候选条件。

## 代码与文档

- [x] 主工程只包含 `app` 和 `pixel-engine`。
- [x] SDK 只生成 `com.purride:pixel-engine:1.0.0` 一个发布坐标。
- [x] public、Metalava 与 JVM ABI baseline 受门禁保护。
- [x] Quickstart、Host、主题、路由、资源、SPI、测试和迁移指南已提供。
- [ ] 合并后的全量 JVM、Lint、instrumentation 与外部消费者回归全部通过。

## 发布与供应链

- [x] Apache-2.0 `LICENSE`、`NOTICE` 与发布元数据已固定。
- [x] 已实现 SBOM、依赖锁、dependency verification、checksum 和签名演练。
- [ ] 正式 Maven namespace、长期签名身份、tag、Release 与真实 publish 留待未来发布 Goal。

性能 benchmark、trace 和 soak 属于后续独立性能 Goal，不是当前清单条件。

```bash
bash tools/pixel-release-check.sh
```

本清单通过只表示本地候选满足门禁，不表示已经对外发布。
