package com.purride.pixelui.animation

/** 枚举 `PixelAnimationStatus` 支持的 `PixelAnimationStatus` 策略，序号不作为持久化协议。 */
public enum class PixelAnimationStatus {
    Dismissed,
    Forward,
    Completed,
    Reverse,
}
